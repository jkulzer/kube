apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: root-certificate-distribution
  # the namespace gets set in the trust-manager values file
spec:
  sources:
  - secret:
      name: "root-certificate"
      key: "tls.crt"
  target:
    configMap:
      key: "kube-home-crt"
