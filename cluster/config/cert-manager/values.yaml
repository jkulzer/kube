namespace: "cert-manager"

clusterResourceNamespace: "cert-manager"

prometheus:
  enabled: true
  servicemonitor:
    enabled: true

installCRDs: true

global:
  leaderElection:
    namespace: cert-manager


startupapicheck:
  enabled: true
  pobLabels:
    sidecar.istio.io/inject: "false"
