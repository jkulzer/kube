namespace: "cert-manager"

clusterResourceNamespace: "cert-manager"

prometheus:
  enabled: true
  servicemonitor:
    enabled: true

installCRDs: true

global:
  leaderElection:
    namespace: cert-manager


startupapicheck:
  enabled: true
  pobAnnotations:
    sidecar.istio.io/inject: "false"
