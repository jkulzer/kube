prometheus:
  namespace: prometheus
  serviceAccount: default
  namespace: prometheus
  podMonitor:
    enabled: true

controller:
  logLevel: info
  resources:
    requests:
      cpu: 10m

speaker:
  logLevel: info
  resources:
    requests:
      cpu: 10m
