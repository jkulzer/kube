apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: homer-authentik-policy
  namespace: homer
spec:
  selector:
    matchLabels:
      istio: gateway 
  action: CUSTOM
  provider:
    name: "authentik"
  rules:
  - to:
    - operation:
        hosts:
          - "homer.kube.home"
