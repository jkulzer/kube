---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ldap
  namespace: authelia
  labels:
    app: grafana
spec:
  volumeMode: Filesystem
  storageClassName: ceph-block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
