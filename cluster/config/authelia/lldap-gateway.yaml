---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: lldap
  namespace: authelia
spec:
  hosts:
  - "ldap.kube.home"
  gateways:
  - istio-system/main-gateway
  http:
  - route:
    - destination:
        host: ldap
        port:
          number: 17170
