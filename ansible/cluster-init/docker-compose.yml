version: "3"
services:
  ansible:
    container_name: ansible
    build:
      context: ../../images/ansible-unified-image/.
    tty: true
    stdin_open: true
    environment:
      ANSIBLE_HOST_KEY_CHECKING: False
    volumes:
      - ./playbook:/playbook
      - ~/.kube:/kube
