apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: sync-argocd-oidc-secret
  annotations:
    policies.kyverno.io/title: Sync argocd-oidc-secret secret from argocd to authelia namespace
    policies.kyverno.io/subject: Secret
    policies.kyverno.io/minversion: 1.6.0
spec:
  mutateExistingOnPolicyUpdate: true
  generateExistingOnPolicyUpdate: true
  rules:
  - name: sync-image-pull-secret
    match:
      any:
      - resources:
          kinds:
          - Namespace
    generate:
      apiVersion: v1
      kind: Secret
      name: argocd-oidc-secret
      namespace: authelia
      synchronize: true
      clone:
        name: argocd-oidc-secret
        namespace: argocd
