---
apiVersion: v1
kind: Service
metadata:
  name: lb-demo
  namespace: load-balancer-demo
  labels:
    app: lb-demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: lb-demo
