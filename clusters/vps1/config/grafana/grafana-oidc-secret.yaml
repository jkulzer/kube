---
apiVersion: secrets.esg.jkulzer.dev/v1alpha1
kind: Secret
metadata:
  name: grafana-oidc-secret
  namespace: grafana
spec:
  secret:
    name: grafana-oidc-secret
    namespace: grafana
  generator:
    type: authelia-hash
    length: 10
    key: GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET
    hashKey: GRAFANA_OAUTH_HASHED_KEY
