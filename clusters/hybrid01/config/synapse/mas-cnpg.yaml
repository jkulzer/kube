---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: mas-pg
  namespace: synapse
spec:
  instances: 1
  bootstrap:
    initdb:
      database: mas
      owner: mas_user
  storage:
    size: 1Gi
